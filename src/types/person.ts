
/**
 * Tipo Person - Representa uma pessoa participante do split
 */
export interface Person {
  id: string;      // Identificador único da pessoa
  name: string;    // Nome da pessoa
  color: string;   // Cor para identificação visual (classes CSS)
}
